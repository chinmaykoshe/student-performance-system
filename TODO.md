# TODO - Styling improvements (white-on-white + theme alignment)

- [ ] Update `student-performance-system/frontend/src/index.css` to remove pure `bg-white` usage in `.glass-panel` and `.glass-card` (use theme-tinted palette instead) to fix white-on-white and improve theme alignment.
- [ ] Verify other hardcoded white usages (if tooling works) and adjust if necessary.
- [ ] Run frontend build/dev check to ensure styling compiles.

